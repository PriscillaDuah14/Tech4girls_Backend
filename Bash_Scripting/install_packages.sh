#!/bin/bash
# Script to install fortune, cowsay, and cmatrix

# Update the package list before installation.

sudo apt update

# Install the packages
sudo apt install cowsay
sudo apt install fortune
sudo apt install cmatrix
